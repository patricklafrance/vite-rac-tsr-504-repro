# Gitleaks configuration — https://github.com/gitleaks/gitleaks
# Extends the default ruleset; only add allowlist entries for confirmed false positives.

[allowlist]
paths = ['''public/mockServiceWorker\.js''', '''pnpm-lock\.yaml''']
